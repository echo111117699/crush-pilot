// OCR service — tesseract.js integration (pending CDN fix)
// Currently disabled; mock screenshots provide inline SVG for analysis demos

export async function extractTextFromImage(_imageUrl) {
  throw new Error('OCR not yet configured. Use mock screenshots for now.')
}
